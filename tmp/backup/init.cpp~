#include <stdio.h>

int fib(int a)
{
	if(a==0)
		return 0;
	if(a==1)
		return 1;
	return fib(a-1)+fib(a-2);
}

int main(void)
{
	int i;	
    for (i = 0; i < 3; ++i) {
		printf("hello world");	
    }
    printf("%d",fib(5));
	return 0;
}
